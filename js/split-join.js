const lyrics = 'Tumi bondhu kala pakhi ami jeno ki, bosonto kale tomai bolte parini, kala kala sada sada';

const partialSlice = lyrics.slice(5, 8);
console.log(partialSlice);

const partialSubString = lyrics.substring(5, 8);
console.log(partialSubString);

const str1 = 'Hello';
const str2 = 'World';

console.log(str1.concat(str2));

const textArray = ['I', ' Love', ' Allah.']

const textJoin = textArray.join();
console.log(textJoin);

